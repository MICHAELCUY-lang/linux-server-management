# Configuration Documentation

## System Health Script Configuration
The `scripts/system-health.sh` script utilizes standard POSIX commands (`df`, `free`, `who`) and requires no external dependency installations. 

To adjust execution intervals or monitoring thresholds, open the script and modify the system utility flags according to your server specifications.